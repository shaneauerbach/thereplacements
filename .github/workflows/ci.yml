name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Customize this section for your tech stack

      # Python example:
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run linting
        run: ruff check .

      - name: Check formatting
        run: ruff format --check .

      - name: Run type checking
        run: mypy src --ignore-missing-imports

      - name: Run tests
        run: pytest --tb=short -q

      # Node.js example (uncomment if using Node):
      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      #
      # - name: Install dependencies
      #   run: npm ci
      #
      # - name: Run linting
      #   run: npm run lint
      #
      # - name: Run tests
      #   run: npm test
