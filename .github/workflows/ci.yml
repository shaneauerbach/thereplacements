name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # ============================================
      # CUSTOMIZE THIS SECTION FOR YOUR TECH STACK
      # ============================================

      # Example: Node.js
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      # - run: npm ci
      # - run: npm run lint
      # - run: npm test

      # Example: Python
      # - uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.11'
      # - run: pip install -e ".[dev]"
      # - run: ruff check .
      # - run: pytest

      # Example: Rust
      # - uses: dtolnay/rust-toolchain@stable
      # - run: cargo fmt --check
      # - run: cargo clippy
      # - run: cargo test

      # Example: Go
      # - uses: actions/setup-go@v5
      #   with:
      #     go-version: '1.21'
      # - run: go fmt ./...
      # - run: go vet ./...
      # - run: go test ./...

      - name: Placeholder
        run: echo "Configure CI for your tech stack in .github/workflows/ci.yml"
